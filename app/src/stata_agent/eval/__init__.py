"""Offline product-evaluation harness.

Use ``python -m stata_agent.eval --json`` (or the installed
``stata-agent-eval --json`` console script) to run the deterministic L1--L4
scenario gates through L6.  The older :mod:`stata_agent.eval.checks` helpers remain
available for focused writer/number checks.
"""
